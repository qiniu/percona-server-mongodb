logging:
  executor:
    format: '[%(name)s] %(asctime)s %(message)s'
    handlers:
      - class: logging.FileHandler
        filename: integrationTest/executor.log
        mode: w
  fixture:
    format: '[%(name)s] %(asctime)s %(message)s'
    handlers:
      - class: logging.FileHandler
        filename: integrationTest/fixture.log
        mode: w
  tests:
    format: '[%(name)s] %(asctime)s %(message)s'
    handlers:
      - class: logging.FileHandler
        filename: integrationTest/tests.log
        mode: w
